<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Frontend | Michael Herman]]></title>
  <link href="http://mherman.org/blog/categories/frontend/atom.xml" rel="self"/>
  <link href="http://mherman.org/"/>
  <updated>2016-09-12T07:16:18-06:00</updated>
  <id>http://mherman.org/</id>
  <author>
    <name><![CDATA[Michael Herman]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Mad Libs With jQuery: A Gentle Introduction to Javascript and jQuery]]></title>
    <link href="http://mherman.org/blog/2014/01/15/mad-libs-with-jquery-a-gentle-introduction-to-jquery/"/>
    <updated>2014-01-15T19:39:00-07:00</updated>
    <id>http://mherman.org/blog/2014/01/15/mad-libs-with-jquery-a-gentle-introduction-to-jquery</id>
    <content type="html"><![CDATA[<p>Did you ever play Mad Libs as a kid? If not, the rules are simple: You fill out a random list of words based on a part of speech - e.g, nouns, verbs, adjectives, and adverbs - without knowing the underlying context. These words are then inserted into a story. When read, the results are generally humorous, causing much laughter.</p>

<ol>
<li>Try it here - <a href="http://www.backwardsteps.com/madlibs/">http://backwardsteps.com/madlibs/</a>.</li>
<li>Grab the repo here - <a href="http://github.com/mjhea0/jquery-madlibs">http://github.com/mjhea0/jquery-madlibs</a></li>
</ol>


<p>Let&rsquo;s have some fun.</p>

<h2>Contents</h2>

<ol>
<li>Create a Project Boilerplate</li>
<li>Add a Form</li>
<li>Handle the Event</li>
<li>Append the text to the DOM</li>
<li>Update the Form and Add the Story</li>
<li>Update the jQuery</li>
<li>Handle Another Event</li>
</ol>


<hr>


<h2>Step 1: Create a Project Boilerplate</h2>

<p>Start by creating the following files and directories, to define a basic project structure:</p>

<blockquote><p><strong>What program are you using to make these files in?</strong> I will be using <a href="http://www.sublimetext.com/">Sublime Text</a> for this tutorial, which is an advanced text editor for Windows, Mac, and Linux. If you are looking for something simpler, check out <a href="https://wiki.gnome.org/Apps/Gedit">gedit</a> - which is also cross-platform. Both of these editors have syntax highlighting. Put simply, syntax highlighting helps distinguish between different parts of a languages syntax (rules), by highlighting common parts the same color. This makes code easier to read and debug.</p></blockquote>

<pre><code class="sh">├── css
│   └── main.css
├── index.html
└── js
    └── main.js
</code></pre>

<h4>index.html</h4>

<p>{% codeblock lang:html %}
&lt;!DOCTYPE html>
<html>
  <head>
    <link href="http://netdna.bootstrapcdn.com/bootswatch/3.0.3/united/bootstrap.min.css" rel="stylesheet" type="text/css" media="all">
    <link href="css/main.css" rel="stylesheet" type="text/css" media="all">
    <title>My AWESOME Boilerplate</title>
  </head>
  <body>
    <div class="container">
      <h1>Hello, World!</h1>
    </div>
    <script src="http://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="http://netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"></script>
    <script src="js/main.js"></script>
  </body>
</html>
{% endcodeblock %}</p>

<h4>main.css</h4>

<pre><code class="css">/* main.css */
</code></pre>

<h4>main.js</h4>

<pre><code class="javascript">$(function() {
  console.log("whee!")
});
</code></pre>

<h3>What&rsquo;s going on?</h3>

<p>This is a good base for us to start with.</p>

<p>Open the &ldquo;index.html&rdquo; file in your web browser. You should see &ldquo;Hello, World&rdquo;.</p>

<p>Go back and look at &ldquo;index.html&rdquo; in your text editor. You&rsquo;re looking at HTML. Put simply, HTML is the language used for creating websites, displayable in a web browser. Now open the &ldquo;main.css&rdquo; file. While HTML provides the structure, CSS makes webpages look pretty. Together, they are the fundamental building blocks for web pages.</p>

<p>So, while we&rsquo;re on the topic of HTML, there are a number of different parts and rules. If I attempted to explain them all, this tutorial would last for days. Fortunately, for this tutorial, you really only need to understand tags, elements, and selectors.</p>

<p>{% codeblock lang:html %}</p>

<div class="container">
  <h1>Hello, World!</h1>
</div>


<p>{% endcodeblock %}</p>

<ol>
<li>Tags form the structure of your page. There usually is an opening tag and then a closing tag, like - <code>&lt;div&gt;&lt;/div&gt;</code>.</li>
<li>Elements represent the tags as well as <em>whatever</em> falls within the tags, like - <code>&lt;h1&gt;Hello, World!&lt;/h1&gt;</code></li>
<li>Selectors are used to select the tag for some purpose. You could use them for defining styles vis CSS or as JavaScript or jQuery hooks - that is, for referencing certain JavaScript code back to the HTML. <em>Put another way, with regard to Javascript, selectors are used to access either tags or elements.</em></li>
</ol>


<blockquote><p>Did you notice that one of my CSS files and two of my scripts are imported from an external URL? These come from a Content Delivery Network (CDN). There are benefits for doing this. See if you can figure them out. Use Google.</p></blockquote>

<p>In the JS file there is a <code>console.log</code>. This is a debugging tool that allows you to post a message to the browser&rsquo;s JavaScript console - e.g. Firebug (Firefox) or Developer Tools (Chrome / Safari). If you&rsquo;ve never used this before, Google &ldquo;accessing the js console in Chrome&rdquo; to learn how to pull up the JavaScript console.</p>

<blockquote><p><strong>What about Internet Explorer? Can I use the debugger in that?</strong> Honestly, I do not know. But the majority of web developers do not use Internet Explorer, because it is simply not developer friendly - it does not have the developer tools that Chrome or Firefox has.</p></blockquote>

<p>Open your console. You should see the text &ldquo;whee!&rdquo;.</p>

<p>Oh - and JavaScript is used in conjunction with HTML and CSS to make webpages interactive.</p>

<h2>Step 2: Add a Form</h2>

<p>For Mad Libs we need to display a form for the user to enter words. Let&rsquo;s start with a simplified version.</p>

<h4>index.html</h4>

<p>Add the following code to your &ldquo;index.html&rdquo; file right after <code>&lt;h1&gt;Hello, World!&lt;/h1&gt;</code>:</p>

<p>{% codeblock lang:html %}</p>

<!-- start form -->


<p><form id="my-form" role="form">
  <input id="my-input" class="form-control" type="text" placeholder="Enter something ..">
  <br>
  <button type="submit" class="btn btn-primary btn-md">My BIG Button</button>
</form></p>

<!-- end form -->


<p>{% endcodeblock %}</p>

<p>Updated file:</p>

<p>{% codeblock lang:html %}
&lt;!DOCTYPE html>
<html>
  <head>
    <link href="http://netdna.bootstrapcdn.com/bootswatch/3.0.3/united/bootstrap.min.css" rel="stylesheet" type="text/css" media="all">
    <link href="css/main.css" rel="stylesheet" type="text/css" media="all">
    <title>My AWESOME Boilerplate</title>
  </head>
  <body>
    <div class="container">
      <h1>Hello, World!</h1>
      <!-- start form -->
      <form id="my-form" role="form">
        <input id="my-input" class="form-control" type="text" placeholder="Enter something ..">
        <br>
      </form>
      <button type="submit" class="btn btn-primary btn-md">My BIG Button</button>
      <!-- end form -->
    </div>
    <script src="http://code.jquery.com/jquery-1.10.2.min.js"></script>
    <script src="http://netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"></script>
    <script src="js/main.js"></script>
  </body>
</html>
{% endcodeblock %}</p>

<h3>What&rsquo;s going on?</h3>

<p>So, here we just added an input and a submit button. Notice all of the new ids and classes. Many of these are associated with <a href="http://getbootstrap.com/">Bootstrap</a> to provide some basic styles.</p>

<p>Let&rsquo;s add some custom styles within our local CSS file, &ldquo;main.css&rdquo;:</p>

<h4>main.css</h4>

<pre><code class="css">/* main.css */

.container {
  padding-top: 50px;
  max-width: 300px;
}
</code></pre>

<h3>What&rsquo;s going on?</h3>

<p>This added the padding to the top of the page and also set a maximum width to the <code>.container</code> class.</p>

<blockquote><p>Find that class in the HTML file. Try tweaking the pixel values to alter the look of the page.</p></blockquote>

<p>Test out the page in you browser. Insert a word into the input box and click the button. Nothing happens. We need to somehow grab that inputted word and do <em>something</em> with it.</p>

<h2>Step 3: Handle the Event</h2>

<p>The process of grabbing the inputted word from the form when a user clicks the button is commonly referred to as an event handler. In this case, the event is the actual button click. We will use jQuery to &ldquo;handle&rdquo; that event.</p>

<p>Please note that jQuery is JavaScript. Well, it&rsquo;s actually a set of libraries developed in JavaScript. But it is possible to perform all the same functionality jQuery provides in vanilla Javascript; it just takes a lot more code.</p>

<blockquote><p>These libraries simplify many of the features in JavaScript such as HTML document (DOM) traversing, event handling, and AJAX using fewer lines of code. Again, utilize Google to find a good tutorial on JavaScript, jQuery, and their differences.</p></blockquote>

<h3>main.js</h3>

<p>Update &ldquo;main.js&rdquo;</p>

<pre><code class="javascript">$(function() {

  console.log("whee!")

  // event handler
  $("#btn-click").click(function(e) {
    var input = $("input").val()
    console.log(input)
  });

});
</code></pre>

<p>Add an id (<code>id="btn-click"</code>) to &ldquo;index.html&rdquo; within the <code>&lt;button&gt;</code> tags:</p>

<p>Before:</p>

<p>{% codeblock lang:html %}
<button type="submit" class="btn btn-primary btn-md">My BIG Button</button>
{% endcodeblock %}</p>

<p>After:</p>

<p>{% codeblock lang:html %}
<button type="submit" id="btn-click" class="btn btn-primary btn-md">My BIG Button</button>
{% endcodeblock %}</p>

<h3>What&rsquo;s going on?</h3>

<ol>
<li><code>$("#btn-click").click(function(e) {</code> is the event. This initiates the process, running the code in the remainder of the function. In other words, the remaining JavaScript/jQuery will not run until there is a button click.</li>
<li><code>var input = $("input").val()</code> sets a variable called &ldquo;input&rdquo;, while <code>.val()</code> fetches the value from the form input.</li>
<li><code>id="btn-click"</code> is used to tie the HTML to the JavaScript. This id is reference in the initial event within the JavaScript file - <code>"#btn-click"</code>.</li>
<li><code>console.log(input)</code> displays the word to the end user via the JavaScript console.</li>
</ol>


<blockquote><p>The <code>$()</code> in <code>$("#btn-click").click()</code> is a jQuery constructor. Basically, it&rsquo;s used to tell the browser that the code within the parenthesis is jQuery.</p></blockquote>

<p>Open &ldquo;index.html&rdquo; in your browser. Make sure you have your JavaScript console open. Enter a word in the input box and click the button. This should display the word in the console:</p>

<p><img src="https://raw.github.com/mjhea0/jquery-madlibs/master/images/console.png" alt="console" /></p>

<h2>Step 4: Append the text to the DOM</h2>

<p>Next, instead of using a <code>console.log</code> to display the inputted word to the user, let&rsquo;s add it to the Document Object Model (DOM)? Wait. What&rsquo;s the DOM? Quit simply, the DOM is a structural representation of the HTML document. Using JavaScript, you can add, remove, or modify the contents of the DOM, which changes how the page looks to the end user.</p>

<p>In this case, we want to add the text to the DOM.</p>

<h4>main.js</h4>

<p>Open up your JavaScript file and add this line of code:</p>

<pre><code class="javascript">$(".results").empty().append(input);
</code></pre>

<p>Updated file:</p>

<pre><code class="javascript">$(function() {

  console.log("whee!")

  // ---- event handler ---- //
  $("#btn-click").click(function(e) {
    // grab the value from the input box after the button click
    var input = $("input").val()
    // display value within the browser's JS console
    console.log(input)
    // add the value to the DOM
    $(".results").append(input);
  });

});
</code></pre>

<h4>index.html</h4>

<p>Add the following lines just below the form:</p>

<p>{% codeblock lang:html %}
<br>
<br></p>

<!-- inputted text after button click -->


<p class="results"></p>


<p>{% endcodeblock %}</p>

<h3>What&rsquo;s going on?</h3>

<p><code>$(".results").append(input);</code> adds the value of the input variable to the DOM between the <code>&lt;p&gt;</code> tags that have the class name &ldquo;results&rdquo; - which is exactly what we added to &ldquo;index.html&rdquo;: <code>&lt;p class="results"&gt;&lt;/p&gt;</code>.</p>

<p>Test this out in your browser. You should see this:</p>

<p><img src="https://raw.github.com/mjhea0/jquery-madlibs/master/images/dom.png" alt="dom" /></p>

<h4>main.js</h4>

<p>Before moving on, we need to make one last update to &ldquo;main.js&rdquo;. Did you notice how each of the words were placed in the DOM next to one another. That looks a bit sloppy. We could add a list to organize it. But let&rsquo;s just display only the last submitted word.</p>

<p>Before:</p>

<pre><code class="javascript">$(".results").append(input);
</code></pre>

<p>After:</p>

<pre><code class="javascript">$(".results").empty().append(input);
</code></pre>

<p>I&rsquo;m sure you can guess what this method does. If not, test it out. Test it out, regardless. :)</p>

<p><strong><em>That&rsquo;s it for the basics. Next, let&rsquo;s move on to adding the Mad Libs functionality.</em></strong></p>

<h2>Step 5: Update the Form and Add the Story</h2>

<p>First, expand the form:</p>

<h4>index.html</h4>

<p>{% codeblock lang:html %}</p>

<!-- start form -->


<div id="questions">
  <h1>Please fill out the following!</h1>
  <hr>
  <form role="form">
    <div class="form-group">
      <label for="person">Boy's Name</label>
      <input id="person" class="form-control person" type="text" placeholder="Enter a boy's name ..">
    </div>
    <div class="form-group">
      <label for="adjective">Adjective</label>
      <input id="adjective" class="form-control adjective" type="text" placeholder="Enter an adjective ..">
    </div>
    <div class="form-group">
      <label for="noun">Plural noun</label>
      <input id="noun" class="form-control noun" type="text" placeholder="Enter a plural noun ..">
    </div>
    <div class="form-group">
      <label for="insect">An insect, plural</label>
      <input id="insect" class="form-control insect" type="text" placeholder="Enter an insect, plural ..">
    </div>
    <div class="form-group">
      <label for="noun2">Plural noun</label>
      <input id="noun2" class="form-control plural-noun" type="text" placeholder="Enter a plural noun ..">
    </div>
    <div class="form-group">
      <label for="verb">A verb ending in "s"</label>
      <input id="verb" class="form-control verb" type="text" placeholder="Enter a verb ending in "s" ..">
    </div>
    <div>
    </div>
  </form>
  <button id="btn-click" type="submit" class="btn btn-primary btn-lg">Generate!</button>
  <hr>
</div>


<!-- end form -->


<p>{% endcodeblock %}</p>

<h3>What&rsquo;s going on?</h3>

<p>Here we added a new form with both labels and inputs. I also updated the page title, <code>&lt;title&gt;Morning Mad Libs&lt;/title&gt;</code>, and removed <code>&lt;h1&gt;Hello, World!&lt;/h1&gt;</code>.</p>

<p>Go ahead and update the <code>max-width</code> of the <code>container</code> in the CSS file to 500px: <code>max-width: 500px;</code>.</p>

<p>Check out the file in the browser:</p>

<p><img src="https://raw.github.com/mjhea0/jquery-madlibs/master/images/form.png" alt="form" /></p>

<h4>index.html</h4>

<p>Next, remove this code-</p>

<p>{% codeblock lang:html %}
<br>
<br></p>

<!-- inputted text after button click -->


<p class="results"></p>


<p>{% endcodeblock %}</p>

<p>-and in it&rsquo;s place add the story:</p>

<p>{% codeblock lang:html %}</p>

<!-- start story -->


<div id="story">
  <h1>Mad with the Libs</h1>
  <hr>
  <h3>I'm in love with <span class="person"></span>. He's so <span class="adjective"></span>! He has big flat <span class="noun"></span>, and when our <span class="insect"></span> meet, I get <span class="noun2"></span> in my stomach. I've fallen for him like a ton of <span class="verb"></span>, and he shuffles for me, too. But I think he's got another girlfriend. What should I do?</h3>
</div>


<!-- end story -->


<p>{% endcodeblock %}</p>

<p>Updated file:</p>

<p>{% codeblock lang:html %}
&lt;!DOCTYPE html>
<html>
  <head>
    <!-- stylesheets -->
    <link href="http://netdna.bootstrapcdn.com/bootswatch/3.0.3/united/bootstrap.min.css" rel="stylesheet" type="text/css" media="all">
    <link href="css/main.css" rel="stylesheet" type="text/css" media="all">
    <title>Morning Mad Libs</title>
  </head>
  <body>
    <div class="container"></p>

<pre><code>  &lt;!-- start form --&gt;
  &lt;div id="questions"&gt;
    &lt;h1&gt;Please fill out the following!&lt;/h1&gt;
    &lt;hr&gt;
    &lt;form role="form"&gt;
      &lt;div class="form-group"&gt;
        &lt;label for="person"&gt;Boy's Name&lt;/label&gt;
        &lt;input for="person" class="form-control person" type="text" placeholder="Enter a boy's name .."&gt;
      &lt;/div&gt;
      &lt;div class="form-group"&gt;
        &lt;label for="adjective"&gt;Adjective&lt;/label&gt;
        &lt;input for="adjective" class="form-control adjective" type="text" placeholder="Enter an adjective .."&gt;
      &lt;/div&gt;
      &lt;div class="form-group"&gt;
        &lt;label for="noun"&gt;Plural noun&lt;/label&gt;
        &lt;input id="noun" class="form-control noun" type="text" placeholder="Enter a plural noun .."&gt;
      &lt;/div&gt;
      &lt;div class="form-group"&gt;
        &lt;label for="insect"&gt;An insect, plural&lt;/label&gt;
        &lt;input id="insect" class="form-control insect" type="text" placeholder="Enter an insect, plural .."&gt;
      &lt;/div&gt;
      &lt;div class="form-group"&gt;
        &lt;label for="noun2"&gt;Plural noun&lt;/label&gt;
        &lt;input id="noun2" class="form-control plural-noun" type="text" placeholder="Enter a plural noun .."&gt;
      &lt;/div&gt;
      &lt;div class="form-group"&gt;
        &lt;label for="verb"&gt;A verb ending in "s"&lt;/label&gt;
        &lt;input id="verb" class="form-control verb" type="text" placeholder="Enter a verb ending in "s" .."&gt;
      &lt;/div&gt;
      &lt;div&gt;
      &lt;/div&gt;
    &lt;/form&gt;
    &lt;button id="btn-click" type="submit" class="btn btn-primary btn-lg"&gt;Generate!&lt;/button&gt;
    &lt;hr&gt;
  &lt;/div&gt;
  &lt;!-- end form --&gt;

  &lt;!-- start story --&gt;
  &lt;div id="story"&gt;
    &lt;h1&gt;Mad with the Libs&lt;/h1&gt;
    &lt;hr&gt;
    &lt;h3&gt;I'm in love with &lt;span class="person"&gt;&lt;/span&gt;. He's so &lt;span class="adjective"&gt;&lt;/span&gt;! He has big flat &lt;span class="noun"&gt;&lt;/span&gt;, and when our &lt;span class="insect"&gt;&lt;/span&gt; meet, I get &lt;span class="noun2"&gt;&lt;/span&gt; in my stomach. I've fallen for him like a ton of &lt;span class="verb"&gt;&lt;/span&gt;, and he shuffles for me, too. But I think he's got another girlfriend. What should I do?&lt;/h3&gt;
  &lt;/div&gt;
  &lt;!-- end story --&gt;

&lt;/div&gt;
&lt;!-- scripts --&gt;
&lt;script src="http://code.jquery.com/jquery-1.10.2.min.js"&gt;&lt;/script&gt;
&lt;script src="http://netdna.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.min.js"&gt;&lt;/script&gt;
&lt;script src="js/main.js"&gt;&lt;/script&gt;
</code></pre>

<p>  </body>
</html>
{% endcodeblock %}</p>

<h3>What&rsquo;s going on?</h3>

<p>Notice the <code>&lt;span&gt;</code> tags. The actual words will be inserted between them once the user clicks the button. Notice how the classes for each span matches the input id in the form. This is not required, but it just makes the naming consistent.</p>

<p>Check it out in your browser.</p>

<h2>Step 6: Update the jQuery</h2>

<p>Now we need to expand our JavaScript file so that upon the event (button click), we grab all the values, then insert them between the empty <code>&lt;span&gt;</code> tags.</p>

<h4>main.js</h4>

<pre><code class="javascript">$(function() {
  console.log("whee!")

  // ---- event handler ---- //
  $("#btn-click").click(function(e) {
    // grab the values from the input boxes, then append them to the DOM
    $(".person").empty().append($("input.person").val());
    $(".adjective").empty().append($("input.adjective").val());
    $(".noun").empty().append($("input.noun").val());
    $(".insect").empty().append($("input.insect").val());
    $(".noun2").empty().append($("input.plural-noun").val());
    $(".verb").empty().append($("input.verb").val());
  });

});
</code></pre>

<h3>What&rsquo;s going on?</h3>

<p>Just like in the previous code -</p>

<ol>
<li><code>.val()</code> grabs the value from the form inputs.</li>
<li><code>.empty()</code> removes any text from that specific class in the story.</li>
<li><code>.append()</code> then adds the new value to the story.</li>
</ol>


<p>This time I did not assign the values to variables; instead I used the inputted values as parameters for the <code>.append()</code> method.</p>

<p>Test this out. Did it work? Were the values added to the story? If all went well, they should have been.</p>

<h4>main.js</h4>

<p>Now, before we move on, let&rsquo;s add some more functionality:</p>

<ol>
<li>First, we do not want the story to be initially displayed - <code>$("#story").hide();</code></li>
<li>Next, we want to show the story after the button click - $(&ldquo;#story&rdquo;).show(); - and empty the form&rsquo;s input boxes - <code>$(':input').val('');</code>.</li>
<li>Finally, let&rsquo;s go ahead and hit all the questions after the form is submitted so the user can just focus on the story - <code>$("#questions").hide();</code>.</li>
</ol>


<p>Update main.js with those additional methods:</p>

<pre><code class="javascript">$(function() {

  // hide the story from view
  $("#story").hide();

  // ---- event handler ---- //
  $("#btn-click").click(function(e) {

    // grab the values from the input boxes, then append them to the DOM
    $(".person").empty().append($("input.person").val());
    $(".adjective").empty().append($("input.adjective").val());
    $(".noun").empty().append($("input.noun").val());
    $(".insect").empty().append($("input.insect").val());
    $(".noun2").empty().append($("input.plural-noun").val());
    $(".verb").empty().append($("input.verb").val());

    // show the story
    $("#story").show();

    // empty the form's values
    $(':input').val('');

    // hide the questions
    $("#questions").hide();

  });

});
</code></pre>

<p>Let&rsquo;s test out the functionality again. Enter values into the form and press Generate. You should see the following if all went well:</p>

<p><img src="https://raw.github.com/mjhea0/jquery-madlibs/master/images/story.png" alt="story" /></p>

<blockquote><p>Please note: Instead of using jQuery, <code>$("#story").hide();</code> to hide the story, you could also use CSS - <code>#story {display: none;}</code></p></blockquote>

<h2>Step 7: Handle Another Event</h2>

<p>Okay. Everything looks good. But what happens if the user wants to play again? Sure s/he could refresh the page, but let&rsquo;s make it easier by adding another event handler.</p>

<h4>main.js</h4>

<pre><code class="javascript">$("#play-btn").click(function(e) {
  $("#questions").show();
  $("#story").hide();
});
</code></pre>

<p>Can you tell what&rsquo;s happening here? Look at previous jQuery code that we added before if you need an explanation.</p>

<h4>index.html</h4>

<p>Finally, add in a button with the an id of <code>play-btn</code> right before the closing div, <code>&lt;/div&gt;</code> in the story section:</p>

<p>{% codeblock lang:html %}</p>

<div>
  <button type="submit" class="btn btn-primary btn-lg" id="play-btn">Play Again?</button>
</div>


<p>{% endcodeblock %}</p>

<p><img src="https://raw.github.com/mjhea0/jquery-madlibs/master/images/story2.png" alt="story2" /></p>

<p>Test time.</p>

<h2>All done?</h2>

<p>Test the program out again. If all works out well, run along and play with your bestest of friends.</p>

<ol>
<li>Try it here - <a href="http://www.backwardsteps.com/madlibs/">http://backwardsteps.com/madlibs/</a>.</li>
<li>Grab the repo here - <a href="http://github.com/mjhea0/jquery-madlibs">http://github.com/mjhea0/jquery-madlibs</a></li>
</ol>


<h2>Challenges</h2>

<p>Too easy? Need some challenges?</p>

<ol>
<li>As with all projects/problems, there are several ways of solving them. Implement a new means.</li>
<li>Add unit and functional tests. Coverage too. What could you test? The question section is hidden after the button click. The event handler on the button click doesn&rsquo;t fire unless all input boxes contain text.</li>
<li>Allow users to add their own stories. Think about what type of medium you could use to store the data. JSON, perhaps? Then where would you store the JSON file? LocalStorage? MongoDB?</li>
<li>Make it easy for two people to play one game together. One person would enter his/her answers, which would be stored. The inputs would clear. Then the next person would do the same. Finally, both stories would appear. Double the amount of laughs.</li>
<li>ENTER button. How could you make it so the user could either click the button or press ENTER? (Add another even for the button click or move the submit button into the form tags and add a <code>.preventDefault()</code> method to the JS file).</li>
<li>Could <code>.toggle()</code> be used somehow instead of two different betweens? Maybe you could toggle the question to hide and the story to show, then vice-versa, with the same button?</li>
</ol>


<p>What else?</p>

<h2>Too hard? Start with the basics.</h2>

<p>Read over the first four steps again make sure to write the actual code. DO NOT copy and paste. Read over it again if you have to for further understanding, then try to implement the Mad Libs game on your own, without looking at the remaining steps.</p>

<p>Have fun. Cheers!</p>
]]></content>
  </entry>
  
</feed>
